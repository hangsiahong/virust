console.log('Virust SSR dashboard initialized');

// The page is already rendered on the server
// Client components (marked with 'use client') are hydrated here
// This enables interactive features like the refresh button
